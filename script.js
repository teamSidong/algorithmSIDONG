function solution(n) {
    var answer = [];
    for(let i=0;i<=n;i++){//i가 계속 돌 떄 
        if(n% i == 0) answer.push(i);
        //약수를 구하는법인 나눴을 때 0이 나오는 숫자를 배열에 넣는다.
    }
    return answer;
    //마지막으로 내가 구하는 값을 return 한다.
    }
    
