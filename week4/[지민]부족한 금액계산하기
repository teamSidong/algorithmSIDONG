function solution(price, money, count) {
    var answer = 0;
    //count번째이용 - *count씩 추가
    for(i =1;i<=count;i++){
        answer += i*price;
    }
    console.log("answer",answer);

    return -(money-answer);
}
