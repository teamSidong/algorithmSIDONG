// 1.나누는 수를 자기자신이 될 때까지 반복
// 2. 나머지가 0이 되게 하는 수를 찾음

function solution(n) {
    let answer = [];
    for(let a=0; a<=n; a++){ //a를 n만큼 반복
        if(n%a===0){ // 어떤 수를 나누었을 때 나머지가 0인 경우 
            answer.push(a); // answer에 a를 담음
        }        
    }
    return answer; // answer 반환
}
